@AGENTS.md
# 우리길(Uri-Gil) 프로젝트 컨텍스트

> Claude Code에서 개발을 이어가기 위한 전체 맥락 문서입니다.
> 이 문서를 CLAUDE.md로 프로젝트 루트에 넣거나, 첫 프롬프트에 붙여넣으세요.

---

## 1. 프로젝트 개요

- **서비스명:** 우리길 (Uri-Gil)
- **슬로건:** "너와 나의 길, 우리가 연결되는 순간"
- **한 줄 소개:** GPS 기반 여행 경로 기록 & 공유 앱. 지도에 핑을 찍어 경로를 계획하고, 각 장소에서 영상을 촬영해 기록하고, 완성된 경로를 다른 사용자와 공유하는 플랫폼.
- **공모전:** 2026 한국관광공사 × 카카오 관광데이터 활용 공모전 (웹·앱 개발 부문)
- **제출 마감:** 2026.09.21 (월) 16:00
- **최종 심사:** 2026.10.28 (PT 발표)
- **플랫폼:** 안드로이드 우선 (iOS는 동일 코드로 추후 확장)

---

## 2. 핵심 목표

1. **경로 기록 & 공유:** 사용자가 여행 경로를 핑으로 기록하고, 촬영한 영상과 함께 공유
2. **분산 관광:** 전주 한옥마을에 몰리는 관광객을 외곽 지역(객리단길, 팔복예술공장, 덕진공원)으로 분산
3. **관광공사 OpenAPI 활용:** 필수 사용 (심사 배점 20점)
4. **카카오 OpenAPI 활용:** 카카오맵 SDK, 카카오 로그인, 카카오톡 공유

---

## 3. 기술 스택

| 항목 | 선택 |
|------|------|
| 프레임워크 | React Native (Expo) |
| 언어 | JavaScript |
| 네비게이션 | @react-navigation/native, bottom-tabs, native-stack |
| 지도 | 카카오맵 SDK (@react-native-kakao/map) 또는 네이버맵 (@mj-studio/react-native-naver-map) |
| 카메라 | expo-camera |
| GPS | expo-location |
| 영상 재생 | expo-av |
| 푸시 알림 | expo-notifications |
| 갤러리 접근 | expo-media-library |
| 캐릭터 애니메이션 | lottie-react-native (선택적) |
| 마켓 등록 | Google Play Store ($25 일회성) |

**참고:** 지도 SDK 사용 시 Expo Go에서는 안 되고 development build가 필요함. 초기에는 웹(`npx expo start --web`)으로 레이아웃 잡고, 지도 기능 넣을 때 development build로 전환.

---

## 4. 앱 사용 흐름

### 여행 전 (계획)
1. 홈 피드에서 다른 사용자의 경로 탐색/추천받기
2. "코스 만들기"로 출발지/도착지 입력
3. 지도에서 가고 싶은 장소에 핑 찍기 (검색 또는 지도 탭)
4. 관광공사 API로 "다른 사람들은 이 다음에 여기 갔어요" 주변 관광지 추천
5. 다른 사용자 경로를 "따라가기"로 내 루트에 복사 가능

### 여행 중 (기록)
6. 핑을 따라 이동, 도착하면 핑 색이 진해지며 방문 표시
7. 해당 장소에서 영상 촬영 (10초 내외)
8. 촬영 후 장소명 입력 (GPS 추천 3개 + 직접 입력 + 건너뛰기)
9. 계획에 없던 장소도 중간에 핑 추가 가능
10. 미방문 핑은 반투명으로 남아있음

### 여행 후 (정리/공유)
11. 클립 관리: 순서 변경, 자르기, 삭제 (간단 편집만)
12. 경로 등록: 이름/카테고리/소개 입력 후 공유
13. 여정 리포트 자동 생성 (통계, 경로 지도, 타임라인)
14. SNS/카카오톡 공유

---

## 5. 핑(마커) 시스템

핑은 3가지 상태를 가짐:
- **방문 완료:** 포인트 컬러(#FF7F5C) 100% 불투명, 체크 표시
- **미방문:** 같은 컬러 20% 투명도
- **새로 추가:** 테두리만 있는 아웃라인

핑 추가 방법 2가지:
- 장소 검색해서 추가
- 지도를 길게 탭해서 해당 위치에 추가

---

## 6. 분산 관광 로직

1. **피드 분산 가중치:** 외곽 장소 포함 코스에 노출 가중치 부여, 한옥마을만 있는 코스는 낮은 가중치
2. **코스 생성 시 외곽 추천:** 한옥마을만 있으면 "객리단길이 도보 15분 거리에요" 넛지
3. **실시간 밀집도 반영:** (가능하면) 관광빅데이터 API로 현재 붐비는 곳 파악 → 한적한 곳 추천

---

## 7. 디자인 시스템

### 방향
- 클린 미니멀, 투어딩(Tourding) 앱 스타일 참고
- 단색 포인트, 이모지 사용 금지, 그라데이션 사용 금지
- 라인 아이콘만 사용 (Lucide 또는 Phosphor 스타일)
- 여백 넉넉하게, 장식 요소 최소화

### 컬러
```javascript
export const COLORS = {
  primary: '#FF7F5C',        // 포인트 (이 색 하나만 강조)
  white: '#FFFFFF',           // 기본 배경
  surface: '#F7F7F7',        // 카드/섹션 배경
  text: '#1A1A1A',           // 주요 텍스트
  textSecondary: '#888888',  // 부가 텍스트
  textTertiary: '#BBBBBB',   // placeholder, 비활성
  border: '#EEEEEE',         // 구분선
  navBar: '#1A1A1A',         // 하단 탭바 (다크)
  navInactive: '#666666',    // 비활성 탭 아이콘
  pinVisited: '#FF7F5C',     // 방문 완료 핑
  pinUnvisited: '#FF7F5C35', // 미방문 핑 (20% 투명)
  success: '#4CAF50',        // 성공/완료
  danger: '#FF4444',         // 삭제/에러/REC
};
```

### 타이포그래피
- 폰트: Pretendard 또는 시스템 기본
- 제목: 20-22px, Bold (700)
- 부제: 14-15px, SemiBold (600)
- 본문: 13-14px, Regular (400)
- 캡션: 11-12px, Regular

### 컴포넌트
- Border radius: 12px (카드), 8px (버튼/입력), 20px (칩)
- 그림자: 거의 없음. 써도 `0 1px 3px rgba(0,0,0,0.06)` 수준
- 하단 네비게이션: 다크(#1A1A1A), 활성 탭은 포인트 컬러

---

## 8. 폴더 구조

```
urigil/
├── App.js
├── src/
│   ├── screens/
│   │   ├── Home/
│   │   │   ├── HomeScreen.js
│   │   │   └── HomeScreen.styles.js
│   │   ├── MyRoute/
│   │   │   ├── MyRouteScreen.js
│   │   │   └── MyRouteScreen.styles.js
│   │   ├── Camera/
│   │   │   ├── CameraScreen.js
│   │   │   └── CameraScreen.styles.js
│   │   ├── ClipManage/
│   │   │   ├── ClipManageScreen.js
│   │   │   └── ClipManageScreen.styles.js
│   │   └── MyPage/
│   │       ├── MyPageScreen.js
│   │       └── MyPageScreen.styles.js
│   ├── components/          # 공용 컴포넌트
│   ├── navigation/
│   │   └── AppNavigator.js  # 탭바 + 스택 네비게이션
│   ├── constants/
│   │   └── colors.js        # 색상 상수
│   ├── api/                  # 서버/관광공사 API 호출
│   └── assets/icons/         # 아이콘
```

각 화면 폴더에 `.js` (로직) + `.styles.js` (스타일) 한 쌍으로 관리.

---

## 9. 화면별 상세 스펙

### 9-1. 홈 화면 (HomeScreen)
- 상단: "우리길" 로고 + 검색/알림 아이콘
- 출발지/도착지 입력 → "코스 만들기" 버튼
- "지금 인기있는 코스" 가로 스크롤 카드 리스트
- 각 카드: 미니맵 + 경로 제목 + 장소 수/거리/시간 + 작성자
- 분산 관광 배너 ("지금 한옥마을이 붐벼요!")
- 하단 탭바 (다크)

### 9-2. 내 루트 화면 (MyRouteScreen) ⭐ 핵심
- 지도가 화면 65% 차지
- 핑 3종 상태 표시 (방문/미방문/신규)
- 핑 사이 경로선 (방문=실선, 미방문=점선)
- 플로팅 "장소 검색" + "지도에서 추가" 버튼
- 하단 바텀시트: 경유지 리스트 + "경로 시작하기" 버튼
- 진행률 표시 ("5곳 중 2곳 방문 완료")

### 9-3. 촬영 화면 (CameraScreen)
- 전체 화면 카메라 뷰파인더
- 상단: 닫기, 현재 위치 표시, 카메라 전환
- 하단: 큰 촬영 버튼 (68px), 갤러리, 플래시
- 촬영 중: 프로그레스 링 + REC 표시
- 촬영 후: 장소명 선택 바텀시트 (GPS 추천 3개 + 직접 입력 + 건너뛰기)
- 핵심 원칙: 5초 안에 촬영 시작 가능, 엄지 하나로 조작

### 9-4. 클립 관리 화면 (ClipManageScreen)
- 클립 리스트: 썸네일 + 장소명 + 촬영시간 + 드래그 핸들
- 편집 기능: 순서 변경(드래그), 자르기(트리밍), 삭제 — 이게 전부
- "경로 완성하기" CTA 버튼

### 9-5. 마이페이지 (MyPageScreen)
- 프로필 정보
- 내가 만든 경로 리스트
- 저장한 경로 리스트
- 설정

### 추가 화면 (스택 네비게이션)
- **스팟 추가 화면:** 장소 검색 + 카테고리 필터 + "다른 사람들은 이 다음에 여기 갔어요" 추천
- **경로 상세 화면:** 다른 사용자 경로 보기 + "따라가기" 버튼
- **경로 등록 화면:** 이름/카테고리/소개 입력 → 공유
- **여정 리포트 화면:** 통계 + 경로 지도 + 타임라인 + SNS 공유

---

## 10. 하단 탭바 구성

| 순서 | 이름 | 아이콘 | 비고 |
|------|------|--------|------|
| 1 | 홈 | 집 라인 아이콘 | |
| 2 | 내 루트 | 지도 핀 라인 아이콘 | |
| 3 | 촬영 | 카메라 아이콘 | 가운데 강조, 포인트 컬러 원형, 위로 올라옴 |
| 4 | 마이 | 사람 라인 아이콘 | |

탭바 배경: #1A1A1A (다크), 활성 아이콘: #FF7F5C, 비활성: #666666

---

## 11. 활용 API

### 한국관광공사 OpenAPI (필수)
1. 국문 관광정보 서비스 — 관광지/숙박/식당 메타데이터
2. 관광지별 연관 관광지 / 두루누비 — 다음 동선 추천
3. 관광빅데이터 정보서비스 — 방문자 수, 인기 관광지
4. 관광사진 정보 — 장소 소개 이미지
5. 특화 데이터 (반려동물/무장애/캠핑) — 맞춤 필터

### 카카오 OpenAPI
- 카카오맵 SDK — 지도, 경로 시각화
- 카카오 로그인 — 간편 회원가입
- 카카오톡 공유 — 경로/리포트 공유

---

## 12. 팀 구성

| 역할 | 담당자 | 담당 영역 |
|------|--------|-----------|
| 디자인 | 우진 | 전체 UI/UX, 캐릭터 디자인 |
| 프론트엔드 | 홈 피드, 코스 탐색, 코스 등록, 여정 리포트, 촬영, 영상 편집, 클립 관리, 로그인
| 프론트엔드 | 현아 | 
| 프론트엔드 | 교은 | 
| 백엔드 | 종현, 채연, 우진(디자인 후 합류) | API, 영상 처리, 추천 알고리즘 |

### 프론트 협업 방식
- GitHub 기능별 브랜치: `feature/home-feed` (현아), `feature/camera` (교은)
- main 브랜치는 항상 정상 작동 유지
- 공용 컴포넌트는 `/components`에서 공유

---

## 13. 마일스톤

| 기간 | 내용 |
|------|------|
| 6/25 ~ 7/06 (2주) | 기획 확정 + 디자인 + React Native 학습 |
| 7/07 ~ 7/27 (3주) | 핵심 기능 개발 (홈, 지도, 촬영, 인증) |
| 7/28 ~ 8/17 (3주) | 심화 기능 (편집, 추천 알고리즘, 리액션) |
| 8/18 ~ 9/07 (3주) | 프론트-백 연동, 플레이스토어 내부 테스트 등록 |
| 9/08 ~ 9/14 (1주) | 비공개 테스트 + 데이터 축적 |
| 9/15 ~ 9/21 (1주) | 정식 출시 + 서류 제출 |
| **9/21 16:00** | **1차 심사 서류 제출 마감** |

---

## 14. 영상 처리 방식

하이브리드 (셋로그 참고):
- **앱에서:** 클립 촬영 + GPS 기록, 미리보기, 순서 변경, 삭제
- **서버에서:** 클립 합치기(인코딩), 자막 삽입, 경로 오버레이, 최종 렌더링

---

## 15. 서버 비용 예상

50~100명 테스트 기준, 월 5~9만원 (AWS) 또는 4~7만원 (Firebase).
프리티어 활용 시 월 3만원 이하 가능.

---

## 16. 심사 기준

### 1차 심사 (서면 + 기능, 100점)
- 서비스 기획력 (구체성, 독창성, 트렌드): 30점
- 서비스 완성도 (기능성, 안정성, 편의성): 30점
- 데이터 활용 적절성 (*관광공사 OpenAPI 필수*): 20점
- 서비스 발전성 (지속성, 확장성): 20점

### 최종심사 (PT, 100점)
- 서비스 적정성: 30점
- 서비스 완성도: 30점
- 서비스 실용성: 25점
- 발표 점수: 15점

---

## 17. 현재 프로젝트 상태

- [x] Expo 프로젝트 생성 (`npx create-expo-app urigil`)
- [x] React Navigation 패키지 설치
- [x] 폴더 구조 생성
- [x] colors.js 생성
- [ ] AppNavigator.js (탭바) 구현
- [ ] 각 화면 빈 틀 생성
- [ ] 홈 화면 UI 구현
- [ ] 내 루트(지도) 화면 구현
- [ ] 촬영 화면 구현
- [ ] 클립 관리 화면 구현
- [ ] 경로 등록/공유 화면 구현

---

## 18. 코드 작성 시 주의사항

1. 화면마다 `.js` + `.styles.js` 쌍으로 분리
2. 색상은 반드시 `COLORS` 상수 사용, 하드코딩 금지
3. 이모지 사용 금지 — 모든 곳에 라인 아이콘
4. 컴포넌트 설명 주석 달기 (신입 개발자가 보고 이해할 수 있게)
5. 각 코드가 무슨 역할인지, 왜 이렇게 작성했는지 세세하게 설명